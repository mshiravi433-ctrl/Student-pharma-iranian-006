# 🚀 راهنمای کامل دیپلوی روی Vercel

از صفر تا آخر. هر قدم می‌گوید **کجا کلیک کنید** و **چه چیزی بزنید**.

⏱ زمان لازم: حدود ۱۵ دقیقه

---

## قدم ۱ — مرج کردن تغییرات در گیت‌هاب

تغییرات فعلاً روی برنچ `arena/019f9aca-student-pharma-iranian-006` است و باید وارد `main` شود.

1. به آدرس زیر بروید:
   ```
   https://github.com/mshiravi433-ctrl/Student-pharma-iranian-006
   ```
2. یک نوار زرد بالای صفحه می‌بینید با متن
   **"arena/019f9aca... had recent pushes"** → دکمه‌ی سبز **`Compare & pull request`** را بزنید.
   > اگر نوار زرد را نمی‌بینید: تب **`Pull requests`** → دکمه‌ی سبز **`New pull request`** →
   > در قسمت `compare:` برنچ `arena/019f9aca-student-pharma-iranian-006` را انتخاب کنید.
3. دکمه‌ی سبز **`Create pull request`** را بزنید.
4. در صفحه‌ی بعد دکمه‌ی سبز **`Merge pull request`** → بعد **`Confirm merge`**.

✅ حالا کدها در `main` هستند.

---

## قدم ۲ — ساخت پروژه در Vercel

1. به [https://vercel.com/new](https://vercel.com/new) بروید (با اکانت GitHub وارد شوید).
2. در لیست ریپازیتوری‌ها دنبال **`Student-pharma-iranian-006`** بگردید و روبه‌رویش
   دکمه‌ی **`Import`** را بزنید.
   > اگر ریپو در لیست نبود: روی **`Adjust GitHub App Permissions`** بزنید و به Vercel
   > اجازه‌ی دسترسی به این ریپازیتوری را بدهید.
3. صفحه‌ی **Configure Project** باز می‌شود. **هیچ چیزی را دست نزنید** — همه‌ی مقادیر
   از فایل `vercel.json` به‌صورت خودکار درست پر می‌شوند:

   | فیلد | مقدار (خودکار) |
   |------|----------------|
   | Framework Preset | `Vite` |
   | Build Command | `npm run build` |
   | Output Directory | `dist` |
   | Install Command | `npm install` |

4. ⚠️ **هنوز روی `Deploy` نزنید!** اول قدم ۳ را انجام دهید.

---

## قدم ۳ — وارد کردن کلیدها (مهم‌ترین قدم)

در همان صفحه‌ی Configure Project، بخش **`Environment Variables`** را باز کنید
(روی فلش کنارش بزنید تا باز شود).

حالا **یکی‌یکی** این ۹ مورد را اضافه کنید. برای هرکدام: اسم را در کادر `Key` و
مقدار را در کادر `Value` بگذارید و **`Add`** را بزنید.

> 📋 **مقادیر واقعی کلیدها کجاست؟**
> در فایل **`.env.local.keys.txt`** در پوشه‌ی پروژه روی کامپیوتر خودتان.
> (این فایل عمداً در گیت ذخیره نمی‌شود تا کلیدها عمومی نشوند.)
> آن را باز کنید و مقدار روبه‌روی هر اسم را کپی کنید.

### کلیدهای هوش مصنوعی

| Key | Value را از کجا بردارم |
|-----|------------------------|
| `GEMINI_API_KEY` | خط `GEMINI_API_KEY=` در `.env.local.keys.txt` |
| `GEMINI_PROJECT_ID` | `135757225029` |
| `MISTRAL_API_KEY` | خط `MISTRAL_API_KEY=` در `.env.local.keys.txt` |
| `AIMLAPI_KEY` | خط `AIMLAPI_KEY=` در `.env.local.keys.txt` |
| `BAZAARLINK_API_KEY` | خط `BAZAARLINK_API_KEY=` در `.env.local.keys.txt` |
| `AINATIVE_API_KEY` | خط `AINATIVE_API_KEY=` در `.env.local.keys.txt` |

### کلید تلگرام

| Key | Value |
|-----|-------|
| `TELEGRAM_BOT_TOKEN` | توکن رباتتان از [@BotFather](https://t.me/BotFather) |

### اختیاری (اگر دارید)

| Key | Value |
|-----|-------|
| `APIECO_TOKEN` | توکن apieco.ir شما (اگر ندارید، اصلاً اضافه نکنید) |
| `OPEN_FDA_API_KEY` | کلید OpenFDA (بدون این هم کار می‌کند، فقط محدودیت تعداد دارد) |

> 💡 **نکته درباره‌ی Environment:** زیر هر متغیر سه گزینه‌ی
> `Production` / `Preview` / `Development` است. **هر سه باید تیک باشند**
> (به‌صورت پیش‌فرض هستند — دست نزنید).

> 💡 اگر کلیدی را وارد نکنید، آن موتور فقط رد می‌شود و نوبت به موتور بعدی می‌رسد.
> برنامه نمی‌شکند. ولی اگر **هیچ‌کدام** را وارد نکنید، همه‌ی جستجوها
> «چیزی پیدا نشد» می‌دهند.

---

> ⚡ **راه سریع‌تر:** کادر Key در Vercel قابلیت paste دسته‌جمعی دارد — می‌توانید کل
> محتوای `.env.local.keys.txt` را یک‌جا کپی کرده و داخل کادر `Key` پیست کنید؛
> Vercel خودش خطوط `KEY=VALUE` را تشخیص می‌دهد و همه را با هم اضافه می‌کند.
> (خطوطی که با `#` شروع می‌شوند را اول پاک کنید.)

## قدم ۴ — Deploy

حالا دکمه‌ی سیاه **`Deploy`** را بزنید.

- حدود ۱ تا ۲ دقیقه طول می‌کشد.
- وقتی تمام شد صفحه‌ی تبریک با تصویر مینی‌اپ نمایش داده می‌شود.
- روی **`Continue to Dashboard`** بزنید.
- آدرس سایت شما بالای صفحه است، چیزی شبیه:
  ```
  https://student-pharma-iranian-006.vercel.app
  ```
  این آدرس را کپی کنید — در قدم‌های بعد لازم است.

---

## قدم ۵ — بررسی سلامت هوش مصنوعی‌ها ✅

این مهم‌ترین تست است. در مرورگر این آدرس را باز کنید
(به‌جای `<آدرس-شما>` دامنه‌ی خودتان را بگذارید):

```
https://<آدرس-شما>.vercel.app/api/ai
```

یک متن JSON می‌بینید شبیه این:

```json
{
  "ok": true,
  "providers": [
    { "provider": "Google Gemini",  "ok": true,  "ms": 812 },
    { "provider": "Mistral AI",     "ok": true,  "ms": 640 },
    { "provider": "AI/ML API",      "ok": false, "ms": 1200 },
    ...
  ]
}
```

**چطور بخوانیمش:**
- اگر بالای صفحه `"ok": true` بود ⇒ 🎉 **همه چیز کار می‌کند.**
- هر موتوری که `"ok": true` دارد یعنی کلیدش سالم است.
- اگر موتوری `"ok": false` بود، اشکالی ندارد — بقیه کار می‌کنند و سیستم خودکار
  می‌رود سراغ بعدی. فقط اگر خواستید درستش کنید، کلید همان یکی را در Vercel چک کنید.
- اگر `"ok": false` کلی بود (همه خراب) ⇒ یعنی کلیدها ذخیره نشده‌اند، برگردید قدم ۳
  و بعد **حتماً Redeploy کنید** (قدم ۷ را ببینید).

---

## قدم ۶ — تست جستجو در خود مینی‌اپ

آدرس سایت را باز کنید و در بخش جستجوی دارو این‌ها را امتحان کنید:

| چیزی که تایپ می‌کنید | انتظار |
|----------------------|--------|
| `metformin` | مونوگراف کامل دارو |
| `دیابت` | پروتکل درمانی بیماری |
| `استامینوفن` | مونوگراف فارسی |
| `کلمه‌ی بی‌معنی مثل asdasd` | فقط پیام «چیزی پیدا نشد» |

---

## قدم ۷ — اتصال تلگرام (برای رسیدن سفارش‌ها)

1. در تلگرام ربات خود را باز کنید و دستور `/start` را بفرستید.
2. این آدرس را در مرورگر باز کنید:
   ```
   https://<آدرس-شما>.vercel.app/api/telegram-init
   ```
3. در خروجی JSON دنبال عدد `id` بگردید (چیزی شبیه `512345678`). آن را کپی کنید.
4. برگردید به Vercel:
   **پروژه → تب `Settings` → منوی چپ `Environment Variables`**
   → یک متغیر جدید اضافه کنید:

   | Key | Value |
   |-----|-------|
   | `TELEGRAM_ADMIN_CHAT_ID` | عددی که کپی کردید |

5. **Redeploy کنید** (خیلی مهم — پایین توضیح داده شده).

---

## 🔄 چطور Redeploy کنیم؟

**هر بار که یک Environment Variable اضافه یا عوض می‌کنید، باید Redeploy کنید**،
وگرنه مقدار جدید اعمال نمی‌شود.

1. در پروژه، تب **`Deployments`** را بزنید.
2. روی آخرین دیپلوی (ردیف اول)، سمت راست، آیکون **`...`** (سه نقطه) را بزنید.
3. گزینه‌ی **`Redeploy`** را انتخاب کنید.
4. در پنجره‌ی باز شده دوباره **`Redeploy`** را بزنید.

---

## قدم ۸ — معرفی آدرس جدید به ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید.
2. دستور `/mybots` → ربات خود را انتخاب کنید.
3. **`Bot Settings`** → **`Menu Button`** → **`Configure Menu Button`**.
4. آدرس سایت Vercel خود را بفرستید:
   ```
   https://<آدرس-شما>.vercel.app
   ```
5. یک اسم برای دکمه بفرستید، مثلاً: `همیار دانشجو`

✅ تمام! مینی‌اپ آماده است.

---

## 🔧 جدول عیب‌یابی

| مشکل | علت | راه‌حل |
|------|-----|--------|
| همه‌ی جستجوها «چیزی پیدا نشد» می‌دهند | کلیدها ذخیره نشده یا Redeploy نکرده‌اید | قدم ۳ را چک کنید، بعد Redeploy |
| `/api/ai` خطای **404** می‌دهد | پوشه‌ی `api/` دیپلوی نشده | مطمئن شوید PR مرج شده و Output Directory روی `dist` است |
| `/api/ai` خطای **504** می‌دهد | همه‌ی موتورها کند بودند | چیزی لازم نیست؛ `maxDuration` روی ۳۰ ثانیه تنظیم شده |
| موتوری `ok: false` است | کلید آن یکی اشتباه/منقضی است | فقط همان کلید را عوض کنید؛ بقیه کار می‌کنند |
| پیام تلگرام نمی‌رسد | `TELEGRAM_ADMIN_CHAT_ID` ست نشده | قدم ۷ را انجام دهید |
| بیلد fail می‌شود | خطای وابستگی | در لاگ Vercel روی تب `Building` خطا را ببینید |

---

## ⚠️ نکته‌ی امنیتی مهم

کلیدهایی که در این فایل و در `.env.local.keys.txt` هستند، **در چت و در کامپیوتر شما لو رفته‌اند**.
توصیه می‌کنم بعد از اینکه همه چیز کار کرد، از پنل هر سرویس کلیدها را
**Revoke / Regenerate** کنید و مقدار جدید را فقط در Vercel وارد کنید.

همچنین توکن ربات تلگرام هنوز به‌عنوان مقدار پیش‌فرض داخل `api/notify.ts` و
`api/telegram-init.ts` در کد هست. بهتر است آن را هم از BotFather با دستور
`/revoke` عوض کنید و فقط در Environment Variables بگذارید.

---

## ✅ چک‌لیست نهایی

- [ ] Pull Request مرج شد
- [ ] پروژه در Vercel Import شد
- [ ] ۷ متغیر محیطی اضافه شد
- [ ] Deploy موفق بود
- [ ] `/api/ai` مقدار `"ok": true` برگرداند
- [ ] جستجوی `metformin` نتیجه داد
- [ ] `TELEGRAM_ADMIN_CHAT_ID` ست و Redeploy شد
- [ ] Menu Button در BotFather تنظیم شد


---

# 🔄 چطور در Vercel رفرش کنیم تا تغییرات جدید بیاید؟

سه نوع «رفرش» وجود دارد. بسته به اینکه چه چیزی عوض شده، یکی را انتخاب کنید.

## حالت ۱ — کد جدید پوش شده (خودکار است)

اگر من کد جدیدی پوش کرده باشم، Vercel **خودش** دیپلوی می‌کند. کاری لازم نیست.

برای دیدنش: پروژه → تب **`Deployments`** → ردیف اول باید `Building` یا
`Ready` با زمان چند دقیقه پیش باشد.

اگر شروع نشده بود، دستی اجرا کنید (حالت ۲).

## حالت ۲ — Redeploy دستی (بعد از تغییر Environment Variables)

⚠️ **این مهم‌ترین مورد است:** وقتی کلید یا متغیری را در Vercel اضافه/عوض می‌کنید،
سایت فعلی آن را نمی‌بیند. حتماً باید Redeploy کنید.

1. وارد پروژه در Vercel شوید.
2. تب **`Deployments`** (بالای صفحه).
3. روی ردیف **اول** (آخرین دیپلوی)، سمت راست، آیکون **`⋯`** (سه نقطه) را بزنید.
4. **`Redeploy`** را انتخاب کنید.
5. ⚠️ در پنجره باز شده، تیک **`Use existing Build Cache`** را **بردارید**
   (تا بیلد کاملاً تازه ساخته شود).
6. دکمه **`Redeploy`** را بزنید و ۱ تا ۲ دقیقه صبر کنید تا وضعیت `Ready` شود.

## حالت ۳ — محتوا تازه نمی‌شود (اخبار / آگهی‌ها)

اخبار پزشکی و آگهی‌های شغلی **در هر درخواست زنده گرفته می‌شوند** و هدر
`Cache-Control: no-store` دارند، پس نیازی به Redeploy ندارند.
اگر محتوای قدیمی می‌بینید، مشکل از **کش مرورگر یا تلگرام** است:

- **در مینی‌اپ:** دکمه‌ی **«به‌روزرسانی»** (فید روزانه) یا
  **«دریافت آگهی‌های جدید»** (کاریابی) را بزنید.
- **در تلگرام:** مینی‌اپ را کامل ببندید و دوباره باز کنید.
  اگر باز هم قدیمی بود: تنظیمات تلگرام → `Data and Storage` → `Clear Cache`.
- **در مرورگر:** `Ctrl + Shift + R` (ویندوز) یا `Cmd + Shift + R` (مک).
- **تست مستقیم بدون کش مرورگر:** آدرس‌های زیر را باز کنید — اگر این‌ها داده‌ی
  تازه دارند یعنی سرور سالم است و فقط کش سمت شماست:
  ```
  https://<آدرس-شما>.vercel.app/api/feed
  https://<آدرس-شما>.vercel.app/api/jobs
  ```

---

## 🧭 نکته درباره برنچ این پروژه

در حال حاضر برنچ پیش‌فرض ریپازیتوری **همین برنچ کاری** است:

```
arena/019f9aca-student-pharma-iranian-006
```

یعنی **نیازی به مرج کردن در `main` نیست** — Vercel مستقیماً از همین برنچ دیپلوی می‌کند.

فقط یک بار در Vercel مطمئن شوید:
**Settings → Git → Production Branch** روی همین نام تنظیم شده باشد.
اگر روی `main` بود، آن را به این برنچ تغییر دهید و یک Redeploy بزنید،
وگرنه تغییرات جدید هیچ‌وقت روی سایت اصلی نمی‌آید.

---

## 🔍 چطور مطمئن شویم نسخه‌ی جدید واقعاً بالا آمده؟

| چه چیزی را چک کنیم | نتیجه‌ی درست |
|---------------------|--------------|
| صفحه‌ی اصلی مینی‌اپ | تیتر باید **«مینی اپ همیار دانشجو»** باشد (بدون «خوش آمدید») |
| منوی پایین صفحه | باید دکمه‌ی **«درباره ما»** دیده شود |
| فید روزانه | باید تب **«خبر پزشکی»** داشته باشد |
| کاریابی | باید دکمه‌ی **«دریافت آگهی‌های جدید»** داشته باشد |
| `/api/ai` | باید `"ok": true` برگرداند |

اگر هرکدام از این‌ها را نمی‌بینید، یعنی نسخه‌ی قدیمی را می‌بینید ⇒ حالت ۲ (Redeploy)
یا پاک کردن کش تلگرام.
